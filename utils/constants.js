const _HOST             = 'localhost';
const _PORT             = 3000;
const _SECRET_JWT_KEY   = 'shared-secret-to-change';

module.exports = {
    HOST:               _HOST,
    PORT:               _PORT,
    SECRET_JWT_KEY:     _SECRET_JWT_KEY
};